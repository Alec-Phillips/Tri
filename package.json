{
  "devDependencies": {
    "@tsconfig/recommended": "1.0.3"
  },
  "scripts": {
    "build-dev": "open 'http://localhost:3033/' && tsc && node dev-server.js",
    "build": "tsc"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
